import './header.css'
import logo from '../../assets/Logo .svg'

const Header = () => {
    return (
        <header>
            <img src={logo} alt='logo'></img>
        </header>

        )
}

export default Header;